#!/usr/bin/env bash
set -euo pipefail

echo "CloudOS project initialized."
echo "Next steps:"
echo "1. Start the local services: docker compose -f infrastructure/docker/docker-compose.yml up -d"
echo "2. Build the server and client components"
echo "3. Create a VM, connect a browser, and verify remote desktop streaming"
